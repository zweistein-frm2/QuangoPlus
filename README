to install use cmd prompt in current dir:
sudo python3 setup.py install


to uninstall :

sudo pip3 uninstall quangoplus


########### for develper ############

create debian package source directory from setup.py using stdeb:

python setup.py --command-packages=stdeb.command debianize
